[alias]
lint = "clippy --workspace --all-targets --all-features -- -D warnings"
fmt-check = "fmt --all -- --check"
# the `--` separator matters: it forwards `codegen` / `--check` to the binary
# instead of letting cargo try to parse them as its own arguments
xtask = "run -p generator --"
